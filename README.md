no 209
======
A meteor app that provides property listing, commenting, etc.

## To run
Add a config.js file to the root and set this one configuration:

mail_url = 'smtp://USERNAME:PASSWORD@SMTPURL:PORT/';
